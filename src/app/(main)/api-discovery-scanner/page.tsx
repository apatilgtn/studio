
// src/app/(main)/api-discovery-scanner/page.tsx

export default function APIDiscoveryScannerPage() {
  return (
    <div className="container mx-auto py-8">
      <h1>API Discovery Scanner Test Page</h1>
      <p>If you see this text, the basic route is working.</p>
      <p>The problem might be within the APIDiscoveryScannerView component or its imports if this page loads but the full feature doesn't.</p>
    </div>
  );
}
